function randNum(min, max) {  
    return Math.floor(
      Math.random() * (max - min) + min
    )
  }
  
  for (var i = 0; i <= 999999999; i += 1) {
    var j = i;
 }
 postMessage(j-randNum(999999974,999999994));